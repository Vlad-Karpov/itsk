1) Система состоит из N приборов. Каждый прибор либо работает либо нет. Он пишет в таблицу когда начал работать,
   и когда закончил. Система считается рабочей, когда хотябы один прибор работл. Определить время работы
   системы в промежуток времени.

2)
        select sum(1) from ...
        select count(*) from ...

        Разные выдадут результаты во всех случаях, или всегда одинаковые?

3) уровни изоляции транзакций
4) 2 способа партиционирования
5) как избежать dad lock
6) фибоначи на транзакт sql

WITH Hierachy(level, fp, fn) AS (
     SELECT 0 AS level, 1 as fp, 1 as fn
     UNION ALL
     SELECT h.level + 1 AS level, h.fn fp, h.fn + h.fp fn
     FROM Hierachy h where h.level < 20) SELECT level, fn
                      FROM Hierachy

   но на собеседовании, на бамажке без всего я бы этого не написал…
   да, и эта муйня ограничена вложенностью в 100 на уровне мелкосовтовской базы данных